# Config

議事録の取得や解析に使用する設定ファイルを置くディレクトリです。
各 YAML ファイルは自治体固有の情報を持ち、主に以下の項目を定義します。

- `fetch_url` - 議事録を取得するためのトップページ URL
- `db_path` - 取得済み議事録を記録する SQLite データベースのパス

新しい自治体に対応させる場合は `<municipality>.yaml` という名前でファイルを追加し、上記の設定を記述してください。

- `setagaya.yaml` - 世田谷区議会用の設定
