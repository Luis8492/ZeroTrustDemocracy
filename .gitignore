__pycache__/
*.pyc
logs/*.log
venv

# Frontend build artifacts
frontend/node_modules/
frontend/dist/
frontend/.svelte-kit/

# Raw fetched minutes are regenerated by fetch.py from per-plugin fixtures
# (sample) or live network (real assemblies in private repos). Don't bloat git.
raw_minutes/
app/raw_minutes/*.txt
app/raw_minutes/*.html

